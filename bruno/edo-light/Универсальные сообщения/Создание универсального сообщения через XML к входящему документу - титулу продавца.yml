info:
  name: Создание универсального сообщения через XML к входящему документу / титулу продавца
  type: http
  seq: 2
  tags:
    - Универсальные сообщения

http:
  method: POST
  url: "{{baseUrl}}/api/v1/incoming-documents/xml/unimsg/:doc_id"
  params:
    - name: doc_id
      value: ""
      type: path
      description: Идентификатор документа (UUID). В документации указан тип integer, однако во всех примерах используется UUID-строка
  body:
    type: multipart-form
    data:
      - name: content
        type: file
        value: []
        description: Файл универсального сообщения в формате XML (DP_UNISOOBSCH)
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Универсальное сообщение успешно создано
    request:
      url: "{{baseUrl}}/api/v1/incoming-documents/xml/unimsg/:doc_id"
      method: POST
      params:
        - name: doc_id
          value: ""
          type: path
          description: Идентификатор документа (UUID). В документации указан тип integer, однако во всех примерах используется UUID-строка
      body:
        type: multipart-form
        data:
          - name: content
            type: file
            value: []
            description: Файл универсального сообщения в формате XML (DP_UNISOOBSCH)
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "id": "cd0ce000-0cd0-0c0c-0f0a-00000e0c000f"
          }
  - name: 400 Response
    description: |
      Ошибка запроса. Типичные сообщения:
      - «Обмен УС со статусом {...} ограничен» — некорректный код УС;
      - «Для документа невозможно создать УС» — создание УС-УвТоч на титул продавца доступно только для входящих документов в определённых статусах;
      - «Некорректное тело запроса» — тело запроса отсутствует;
      - «Значение элемента ИдОтпрУС / ИдПолучУС не совпадает с данными отправителя / получателя, указанными в ИдФайл»;
      - «Данные отправителя / получателя УС не совпадают с данными получателя / отправителя документа {filename}»;
      - «Не найден файл {Документ.СведУС.ИмяПолФайл} на который ссылается УС»;
      - «Имя загружаемого файла: {filename} не соответствует значению атрибута ИдФайл».

      Полный перечень ошибок см. в документации ЭДО Лайт.
    request:
      url: "{{baseUrl}}/api/v1/incoming-documents/xml/unimsg/:doc_id"
      method: POST
      params:
        - name: doc_id
          value: ""
          type: path
          description: Идентификатор документа (UUID). В документации указан тип integer, однако во всех примерах используется UUID-строка
      body:
        type: multipart-form
        data:
          - name: content
            type: file
            value: []
            description: Файл универсального сообщения в формате XML (DP_UNISOOBSCH)
    response:
      status: 400
      statusText: Bad Request
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "message": "",
            "error_message": "",
            "code": "",
            "description": ""
          }
  - name: 401 Response
    description: "Ошибка авторизации — токен не прошёл проверку, истёк срок жизни токена или запрос выполнен без токена («Access denied: token not valid» / «Access denied: token not found»)"
    request:
      url: "{{baseUrl}}/api/v1/incoming-documents/xml/unimsg/:doc_id"
      method: POST
      params:
        - name: doc_id
          value: ""
          type: path
          description: Идентификатор документа (UUID). В документации указан тип integer, однако во всех примерах используется UUID-строка
      body:
        type: multipart-form
        data:
          - name: content
            type: file
            value: []
            description: Файл универсального сообщения в формате XML (DP_UNISOOBSCH)
    response:
      status: 401
      statusText: Unauthorized
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "message": "",
            "error_message": "",
            "code": "",
            "description": ""
          }
  - name: 403 Response
    description: «Указанная в токене роль не разрешена при работе с документом»
    request:
      url: "{{baseUrl}}/api/v1/incoming-documents/xml/unimsg/:doc_id"
      method: POST
      params:
        - name: doc_id
          value: ""
          type: path
          description: Идентификатор документа (UUID). В документации указан тип integer, однако во всех примерах используется UUID-строка
      body:
        type: multipart-form
        data:
          - name: content
            type: file
            value: []
            description: Файл универсального сообщения в формате XML (DP_UNISOOBSCH)
    response:
      status: 403
      statusText: Forbidden
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "message": "",
            "error_message": "",
            "code": "",
            "description": ""
          }
  - name: 404 Response
    description: «Документ не найден в БД» — не найден документ, для которого формируется универсальное сообщение
    request:
      url: "{{baseUrl}}/api/v1/incoming-documents/xml/unimsg/:doc_id"
      method: POST
      params:
        - name: doc_id
          value: ""
          type: path
          description: Идентификатор документа (UUID). В документации указан тип integer, однако во всех примерах используется UUID-строка
      body:
        type: multipart-form
        data:
          - name: content
            type: file
            value: []
            description: Файл универсального сообщения в формате XML (DP_UNISOOBSCH)
    response:
      status: 404
      statusText: Not Found
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "message": "",
            "error_message": "",
            "code": "",
            "description": ""
          }
  - name: 500 Response
    description: Внутренняя ошибка сервиса / сервис недоступен
    request:
      url: "{{baseUrl}}/api/v1/incoming-documents/xml/unimsg/:doc_id"
      method: POST
      params:
        - name: doc_id
          value: ""
          type: path
          description: Идентификатор документа (UUID). В документации указан тип integer, однако во всех примерах используется UUID-строка
      body:
        type: multipart-form
        data:
          - name: content
            type: file
            value: []
            description: Файл универсального сообщения в формате XML (DP_UNISOOBSCH)
    response:
      status: 500
      statusText: Internal Server Error
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "message": "",
            "error_message": "",
            "code": "",
            "description": ""
          }

docs: |
  Метод предназначен для создания универсального сообщения (УС) в формате XML к входящему документу / титулу продавца.
  УС — вспомогательный электронный документ для передачи информации о событиях, связанных с основным документом.

  Коды статусов УС см. в справочнике «Коды универсальных сообщений» документации ЭДО Лайт.

  Примечание: создание УС доступно только на интеграционном стенде. С 1 июля 2026 года будет осуществлён переход текущих методов
  на методы True API (см. «Планируемые изменения в True API»: https://docs.crpt.ru/gismt/Exchange/).

  Создание УС-ИзвПол (11519991) и УС-комментариев (11540021) доступно для входящих документов в любом статусе.
  Создание УС-УвТоч на титул продавца (11529991) доступно только для входящих документов в статусах:
  2, 3, 12, 13, 4, 61, 62, 63, 7, 18, 19, 64, 65, 66 (см. справочник «Статусы документов»).
